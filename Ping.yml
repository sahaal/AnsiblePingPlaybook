---

- hosts: ec2
  become: true
  gather_facts: false

  tasks:
    - name: Sleep for 120 seconds and continue with play
      wait_for: timeout=120
      delegate_to: localhost
      
    - name: ping
      ping:

